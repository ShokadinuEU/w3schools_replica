extends ../_component/layout/template

block cool-config
  - 
    var myTitle = "What is HTML?"
    var myDescription = "HTML is the standard markup language for creating Web pages."
    var landingPage = 'HTML';
    var activeLink = '';

block awesome-content
  h1=myTitle
  h5=myDescription
  h2 HTML ELements
  p An HTML element usually consists of a start tag and end tag, with the content inserted in between.
  h2 HTML Attributes 
  p Attributes provide additional information about HTML elements.
  h2 HTML Headings
  p Headings are defined with the h1 to h6 tags.
  h2 HTML Paragraphs
  p The HTML p element defines a paragraph:
