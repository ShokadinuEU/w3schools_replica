extends ../../_component/layout/template

block cool-config
  - 
    var myTitle = "Javascript CSS"
    var myDescription = "JavaScript HTML DOM - Changing CSS"
    var landingPage = 'JavaScript';
    var activeLink = '/javascript/css';

block awesome-content
  h1=myTitle
  h5=myDescription
  h2 Changing HTML Style
  p To change the style of an HTML element, use this syntax:
    | document.getElementById(id).style.property = new style
  h2 Using Events
  p The HTML DOM allows you to execute code when an event occurs.
  p Events are generated by the browser when "things happen" to HTML elements:
  ul
    li An element is clicked on
    li The page has loaded
    li Input fields are changed
  h2 HTML DOM Style Object Reference
  p For all HTML DOM style properties, look at our complete HTML DOM Style Object Reference.